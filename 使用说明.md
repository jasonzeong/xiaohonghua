# 宝贝小红花银行 · iOS PWA 使用说明

## 一、先搞清楚一件事：iPhone 必须走 HTTPS

这是 Apple 的硬性规定，绕不过去：

| 打开方式 | iPhone 能用吗 | 能装桌面图标吗 | 能离线用吗 |
|---|---|---|---|
| 双击 `index.html`（file://） | ❌ 服务框架不工作 | ❌ | ❌ |
| 电脑开服务器，手机连 `http://192.168.x.x` | ⚠️ 能打开，但不稳定 | ❌ 装不了 | ❌ |
| **部署到 HTTPS 网址** | ✅ | ✅ | ✅ |

所以：**只要花 5 分钟部署一次，之后 iPhone / iPad /安卓 / 电脑就都能装成 App 了**，一劳永逸。下面两个方案任选一个，都是免费的。

---

## 二、方案 A：Netlify Drop（最快，推荐先试）

不需要注册、不需要命令行，拖一下文件夹就行。

1. 打开 <https://app.netlify.com/drop>
2. 把 **`小红花PWA` 这个文件夹整个**（注意是文件夹，不是单个文件）拖进网页中间的虚线框
3. 等进度条跑完，会得到一个网址，形如 `https://xxxx-xxxx-xxxx.netlify.app`
4. 点这个网址，确认页面能正常打开
5. **建议右上角 Sign up 注册一个账号**（用邮箱即可，免费），否则这个站点没人认领，过一段时间可能被清理

> Netlify 国内访问速度一般，偶尔会慢。如果觉得卡，换方案 B。

---

## 三、方案 B：GitHub Pages（永久免费，推荐长期用）

你有 GitHub 账号（jasonzeong），直接走网页操作，不用敲命令。

### 步骤

1. 登录 <https://github.com>，右上角 `+` → **New repository**
2. Repository name 填 `xiaohonghua`，选 **Public**，勾选 **Add a README file**，点 **Create repository**
3. 进到仓库页面，点 **Add file** → **Upload files**
4. 把 `小红花PWA` 文件夹**里面的全部内容**拖进去：
   - `index.html`
   - `sw.js`
   - `manifest.webmanifest`
   - `icons` 整个文件夹（GitHub 网页支持直接拖文件夹）
5. 拉到页面最下方，**Commit changes**（标题随便写，比如 `init`）
6. 点仓库顶部的 **Settings** → 左侧 **Pages**
7. Source 选 **Deploy from a branch**，Branch 选 `main`，文件夹选 **/ (root)**，点 **Save**
8. 等 1~2 分钟，刷新页面，顶部会显示网址：
   `https://jasonzeong.github.io/xiaohonghua/`
9. 用电脑浏览器打开确认能显示，就可以拿手机装了

> 以后要改内容：回到仓库 → 点文件 → 右上角铅笔图标编辑 → 最下面 Commit changes。改完等 1 分钟自动生效。

---

## 四、iPhone 装成 App（关键步骤）

拿到 HTTPS 网址后，在 **iPhone 的 Safari 浏览器**里操作（微信里打不开，必须是 Safari）：

1. Safari 打开你的网址，比如 `https://jasonzeong.github.io/xiaohonghua/`
2. 点**屏幕底部中间的分享按钮** ⬆️（方框往上箭头那个）
3. 在弹出菜单里往下滑，找到 **「添加到主屏幕」**
4. 右上角改名字（默认「小红花银行」），点 **添加**
5. 回到桌面，就能看到粉色小红花图标了 ✅

**装好后的效果：**

- 打开没有 Safari 地址栏，跟真 App 一样全屏
- 断网也能用（数据存在手机本地）
- 图标、启动画面都是配好的

---

## 五、多设备同步（爸爸妈妈各自加花，不会互相覆盖）

同步后端用的是你自己 Cloudflare 账号里的存储（不走任何第三方），**不需要注册别的网站、不需要填 Key**。

### 开同步（只要做一次）

1. 手机或电脑打开 App，输入密码 `1234` 进入家长模式
2. 拉到底部找到 **☁️ 家庭多设备同步** 卡片
3. 点 **🚀 创建新的家庭码** → 会生成一串家庭码
4. **另一台设备**（比如妈妈的手机）也打开 App → 家长模式 → 填同一个家庭码 → 点 **🔗 连接**

两边就打通了。之后谁加花、谁打卡，20 秒内自动同步（也能点状态条手动「立即同步」）。

### 为什么不会算错

红花数量不是存一个数字，而是存**流水账**。爸爸加 3 朵、妈妈同时加 5 朵，同步时是两条流水合并，结果是 8 朵，不会互相覆盖。断网状态下各自操作，联网后合并也不会重复计数。

> 不改密码、不联网的情况下，两台设备各自记各自的，联网自动合并。这是设计好的，不是 bug。

---

## 六、日常使用

| 想做的事 | 怎么操作 |
|---|---|
| 加/减红花 | 家长模式 → 点孩子卡片上的 **+ / −**，选数量 |
| 每日任务打卡 | 家长模式 → ✅ 今日任务打卡 → 点任务项，自动加花 |
| 取消打卡 | 再点一次任务项，花会扣回去 |
| 兑换奖励 | 家长模式 → 🎁 奖励兑换 → 点「换」，自动减花 |
| 改密码 | 家长模式 → 底部 ⚙️ 设置 |
| 看流水/导出 | 家长模式 → 底部 流水记录，可导出 CSV / JSON |
| 小朋友自己看 | 首页点 **👶 小朋友看看（只读）**，看不到任何操作按钮 |

默认密码 **1234**，建议装好后在设置里改掉。

---

## 七、常见问题

**Q：装到桌面后，我更新了网站内容，手机还是旧的？**

联网状态下每次打开都会去拿最新页面，所以**把 App 从后台划掉重开一次**就是最新版。如果改了图标这类底层配置，顶部会飘出「🔄 新版本可用，点此刷新」，点一下即可。

**Q：为什么强调一定要加到主屏幕？**

除了体验更像 App，还有个实际原因：**iPhone 上的 Safari 如果 7 天不访问某个网站，会自动清掉它存的数据**。但加到主屏幕的 App 不受这条规则影响，数据一直都在。所以装了主屏幕 = 数据更安全。

**Q：换手机了，数据怎么办？**

有开同步的话，新手机装好 App，填同样的家庭码，数据自动拉回来。没开同步的话，在旧手机的设置里**导出 JSON**，把文件传到新手机导入。

**Q：安卓能用吗？**

能。Chrome 打开网址会自动弹安装提示，或者点右上角菜单 →「安装应用」。

**Q：数据存在哪？安全吗？**

本机数据存在浏览器 localStorage 里（离线也能用）。开了同步会把数据存到你自己的 Cloudflare 账号（xiaohonghua 项目的 KV），只有知道家庭码的人能读到，不经过任何第三方。

**Q：清了浏览器缓存数据会没吗？**

会。所以**强烈建议开同步**，或者定期在设置里导出 JSON 备份。

---

## 八、文件清单

```
小红花PWA/
├── index.html              主程序（单文件，全部逻辑都在这里）
├── sw.js                   离线缓存服务，别删
├── manifest.webmanifest    App 配置（名字/图标/全屏）
├── icons/                  App 图标，别删
│   ├── icon-192.svg        安卓 / 桌面图标（任意尺寸自适应）
│   ├── icon-512.svg        大图
│   ├── maskable-512.svg    安卓自适应图标（带安全边距）
│   └── favicon.svg         浏览器标签
├── 本地预览.bat             电脑上双击预览用（不是装手机用的）
└── 使用说明.md              本文件
```

> 根目录还有一份 `小红花.html`，是同一个程序的单文件版，电脑上双击就能用，但**没有离线和安装功能**。手机上请用 `小红花PWA` 这个文件夹。
